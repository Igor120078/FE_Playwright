import { ComponentInterface } from './componentInterface.js'

/**
 * Represents general behavior of any loading indicator which might apply
 * to whole page or just a single element.
 */
export interface LoadingIndicator extends ComponentInterface<LoadingIndicator> {}
